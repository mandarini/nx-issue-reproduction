import { Typography as UiTypography, TypographyProps } from '@myorg/common-ui-buildable';
import React, { FC } from 'react';

const Typography: FC<TypographyProps> = props => <UiTypography {...props} />;

export default Typography;
