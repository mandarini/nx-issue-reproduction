// button.component.tsx

import React from 'react';
import { Button as UiButton, ButtonProps } from '@myorg/common-ui-buildable';

export const Button = (props: ButtonProps) => <UiButton {...props}/>;